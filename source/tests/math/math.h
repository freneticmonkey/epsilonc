#pragma once

#include <gtest/gtest.h>

#include <math/Vector.h>

TEST(Math, Bounds)
{

}

TEST(Math, Vector2)
{
	Vector2 a(1.0f, 2.0f);
	Vector2 b(3.0f, 4.0f);

	/*
	Vector2 c = a + b;
	EXPECT_EQ(4.0f, c.x);
	EXPECT_EQ(6.0f, c.y);	
	*/
}